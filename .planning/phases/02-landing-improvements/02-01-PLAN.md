---
phase: 02-landing-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - style.css
  - js/main.js
  - js/pages.js
autonomous: false
requirements:
  - NAV-01
  - NAV-02
  - NAV-03

must_haves:
  truths:
    - "Navbar shrinks to a floating pill with border-radius when user scrolls past 80px on index.html"
    - "Floating pill navbar appears on all 20 pages (index + 19 sub-pages)"
    - "Mobile hamburger menu still opens and shows all links when navbar is in pill state"
  artifacts:
    - path: "style.css"
      provides: "Navbar .scrolled styles including pill container, mobile override"
      contains: ".navbar.scrolled"
    - path: "js/main.js"
      provides: "Scroll listener toggling .scrolled class on navbar"
      contains: "scrolled"
    - path: "js/pages.js"
      provides: "Same scroll listener for sub-pages"
      contains: "scrolled"
  key_links:
    - from: "js/main.js"
      to: ".navbar"
      via: "classList.toggle scrolled at 80px"
      pattern: "scrolled.*scrollY"
    - from: "js/pages.js"
      to: ".navbar"
      via: "classList.toggle scrolled at 80px"
      pattern: "scrolled.*scrollY"
---

<objective>
Implement the AmpleMarket-style floating pill navbar that applies to ALL 20 pages.

Purpose: Modern, premium navigation feel. Navbar starts full-width at top and shrinks to ~900px rounded pill on scroll.

Output: Updated style.css with .navbar.scrolled styles, updated js/main.js and js/pages.js with scroll listeners.
</objective>

<execution_context>
@C:/Users/Dell 5520/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dell 5520/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-landing-improvements/2-CONTEXT.md
@.planning/phases/02-landing-improvements/02-RESEARCH.md

<interfaces>
<!-- Existing navbar HTML (same in all 20 pages): nav.navbar > div.container > logo + nav-links + btn + hamburger -->
<!-- CRITICAL: .navbar CSS appears TWICE in style.css -->
<!-- Line ~204: main definition (position: sticky, transition: all 0.5s ease) -->
<!-- Line ~2556: augmented with border-bottom: 1px solid rgba(197, 237, 54, 0.12) -->
<!-- .scrolled styles MUST be placed AFTER line 2556 to win the cascade -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add navbar floating pill CSS and scroll JS to all pages</name>
  <files>style.css, js/main.js, js/pages.js</files>
  <action>
  **CSS (style.css) -- append AFTER line 2556 (after the second .navbar block):**

  Add .navbar .container base transition for smooth pill animation:
  transition properties: max-width, border-radius, padding, background, box-shadow -- all 0.4s cubic-bezier(0.4, 0, 0.2, 1).

  Add .navbar.scrolled: padding 6px 0, background transparent, border-bottom none.

  Add .navbar.scrolled .container: max-width 900px, background rgba(248,247,245,0.97), border-radius 50px, border 1px solid rgba(229,229,229,0.6), box-shadow 0 8px 32px rgba(0,0,0,0.10), padding 8px 24px, backdrop-filter blur(24px), -webkit-backdrop-filter blur(24px).

  **Mobile override** -- verify hamburger breakpoint in style.css first (grep for hamburger media query). At @media (max-width: 768px): .navbar.scrolled .container gets border-radius 0, max-width 100%, padding 8px 16px, border none, box-shadow 0 4px 16px rgba(0,0,0,0.08).

  **Reduced-motion guard:** @media (prefers-reduced-motion: reduce) { .navbar .container { transition: none; } }

  **JS (js/main.js)** -- add inside DOMContentLoaded or init block:
  const navbar = document.querySelector('.navbar');
  if (navbar) {
    window.addEventListener('scroll', () => {
      navbar.classList.toggle('scrolled', window.scrollY > 80);
    }, { passive: true });
  }

  **JS (js/pages.js)** -- add the SAME scroll listener. pages.js is loaded by all 19 sub-pages.

  **PITFALLS:**
  - Do NOT animate .navbar width/max-width -- only animate .navbar .container to preserve position:sticky context
  - Mobile .nav-links.open must NOT be clipped by pill border-radius -- mobile override removes it
  - Place .navbar.scrolled CSS AFTER line 2556 in style.css
  </action>
  <verify>
    <automated>cd "C:/Users/Dell 5520/Documents/AgenticWorkflows/Landing Page" &amp;&amp; grep -c "navbar.scrolled" style.css &amp;&amp; grep -c "scrolled" js/main.js &amp;&amp; grep -c "scrolled" js/pages.js</automated>
  </verify>
  <done>
  - .navbar.scrolled CSS after line 2556 with pill styles
  - Mobile override removes border-radius at hamburger breakpoint
  - js/main.js and js/pages.js both have scroll listener at 80px
  - prefers-reduced-motion disables transition
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify floating navbar on desktop and mobile</name>
  <files>style.css, js/main.js, js/pages.js</files>
  <action>Human verifies the navbar pill animation visually across pages and screen sizes.</action>
  <what-built>Floating pill navbar on scroll for all 20 pages. Full-width at top, shrinks to 900px rounded pill after 80px scroll.</what-built>
  <how-to-verify>
    1. Run npm start and open http://localhost:3000
    2. Scroll down -- navbar smoothly transitions to rounded pill shape
    3. Scroll back to top -- returns to full width
    4. Check http://localhost:3000/pages/precios.html -- same pill behavior
    5. Resize to mobile (375px) -- no pill effect, hamburger works normally
    6. Open hamburger while scrolled -- all nav links visible, no clipping
  </how-to-verify>
  <resume-signal>Type approved or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Pill appears at 80px scroll on index.html and sub-pages
- Mobile hamburger works with no clipping
- Smooth 0.4s cubic-bezier transition
</verification>

<success_criteria>
- NAV-01: Navbar shrinks to pill on scroll >80px
- NAV-02: Works on all 20 pages
- NAV-03: Mobile hamburger still works
</success_criteria>

<output>
After completion, create .planning/phases/02-landing-improvements/02-01-SUMMARY.md
</output>