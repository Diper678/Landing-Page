---
phase: 02-landing-improvements
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - index.html
  - style.css
  - js/main.js
autonomous: false
requirements:
  - ANIM-01
  - ANIM-02
  - ANIM-03
  - ANIM-04

must_haves:
  truths:
    - "User sees animated gradient orbs floating behind the hero section"
    - "All sections on index.html fade-in and slide-up as they enter the viewport"
    - "Feature cards and bento cards glow with accent color border on hover"
    - "A subtle radial gradient follows the mouse cursor on desktop"
    - "Old Como Funciona section replaced by dark bento grid with 5 capability cards"
  artifacts:
    - path: "index.html"
      provides: "Hero orbs HTML, bento grid section replacing old Como Funciona"
      contains: "hero-orb"
    - path: "style.css"
      provides: "Orb keyframes, bento grid layout, card glow hover, cursor glow"
      contains: ".hero-orb"
    - path: "js/main.js"
      provides: "GSAP scroll reveals, cursor glow init, bento stagger"
      contains: "initCursorGlow"
  key_links:
    - from: "style.css"
      to: "index.html .hero-orbs"
      via: "CSS @keyframes orbFloat animations"
      pattern: "orbFloat"
    - from: "js/main.js"
      to: "index.html sections"
      via: "GSAP ScrollTrigger fromTo with once:true"
      pattern: "scrollTrigger.*once"
    - from: "js/main.js"
      to: "document.body .cursor-glow"
      via: "mousemove listener with requestAnimationFrame"
      pattern: "cursor-glow"
    - from: "js/main.js"
      to: "index.html .bento-card"
      via: "GSAP ScrollTrigger stagger"
      pattern: "bento-card"
---

<objective>
Replace the Como Funciona section with a bento grid and add the full animation layer to index.html: hero gradient orbs, GSAP scroll reveals, card hover glow, cursor-following glow, and bento card stagger.

Purpose: Transform the static landing page into a dynamic, modern experience matching competitors (Firecrawl orbs, Linear bento). Animations create depth and convey premium quality.

Output: Bento grid in index.html, animation CSS/JS in style.css and main.js.
</objective>

<execution_context>
@C:/Users/Dell 5520/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dell 5520/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-landing-improvements/2-CONTEXT.md
@.planning/phases/02-landing-improvements/02-RESEARCH.md
@.planning/phases/02-landing-improvements/02-01-SUMMARY.md

<interfaces>
<!-- Existing animated-bg (style.css lines 141-187) -- orbs are COMPANION, not replacement -->
<!-- .animated-bg::before: 600x600px, rgba(197,237,54,0.07), fixed position, bgFloat 20s -->

<!-- Hero section (index.html lines 62-248): -->
<!-- header.hero#hero > div.hero-grid > div.hero-text + div.hero-form-wrapper -->
<!-- Hero MUST get position:relative and overflow:hidden for absolute orbs -->
<!-- .hero-grid needs z-index:2 to sit above orbs -->

<!-- Como Funciona to REPLACE (index.html lines 251-437): -->
<!-- 5x solution-detail blocks with alternating solution-detail--dark -->

<!-- Existing GSAP pattern (main.js lines 395-427): -->
<!-- ScrollTrigger.create with trigger, start, onEnter, once:true -->
<!-- gsap.fromTo with y, opacity, scale, stagger, ease power3.out -->

<!-- Existing custom cursor (main.js lines 490-534): -->
<!-- 24px border circle at z-index:10000 -- cursor glow is separate at z-index:1 -->

<!-- Brand: #c5ed36 accent, #111 dark, #F8F7F5 light, Sharp Grotesk, JetBrains Mono -->

<!-- ui-ux-pro-max skill rules: cursor-pointer on clickable elements, focus-visible states, -->
<!-- prefers-reduced-motion, 44x44px min touch targets, no emojis as icons, transform animations -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bento grid HTML/CSS + hero orbs + card glow + cursor glow CSS</name>
  <files>index.html, style.css</files>
  <action>
  **Part A -- Replace Como Funciona with Bento Grid (index.html):**

  1. Remove the entire section id=como-funciona block (lines 251-437).
  2. Replace with bento grid HTML:
     - section id=como-funciona class=bento-section
     - Container with section-label "Como Funciona", section-title with gradient-text span "automatizacion inteligente", section-desc
     - .bento-grid container (CSS Grid)
     - **Hero card (2x2):** a href=pages/soluciones.html#data-quality class="bento-card bento-card--hero" aria-label="Prospeccion con IA - Ver detalles"
       - .bento-card-icon (Lucide search 40px), .bento-card-label "01 - Prospeccion"
       - h3 "Prospeccion con IA que filtra por ti", p about 100-point scoring motor
       - .bento-flow-diagram: 3 nodes (LinkedIn -> brain/IA -> labels) with animated connectors
       - .bento-flow-labels: HOT (#c5ed36), WARM (#f5c542), SKIP (gray) pills
       - 2-3 .bento-feature items with check icons
       - .bento-stats: "5-7x Mas conversiones", "-70% Costo por lead"
       - .bento-arrow with arrow-right icon
     - **Card 2 (1x1) Secuencias:** href=#ai-247, icon mail, "02 - Secuencias", features about 5 emails 35 days 9AM, stats "5 Emails"/"78% Compra al 1ro"
     - **Card 3 (1x1) Omnicanal:** href=#omnichannel, icon share-2, "03 - Omnicanal", features about unified pipeline, stats "89% Retencion"/"+40% Respuestas"
     - **Card 4 (1x1) Analisis:** href=#analytics, icon bar-chart-3, "04 - Analisis", features about dashboards, stats "360 Pipeline"/"Real-time"
     - **Card 5 (1x1) Seguridad:** href=#security, icon shield-check, "05 - Seguridad", features about Ley 21.719, stats "Ley 21.719"/"E2E"
     - Each card: .bento-card-icon + .bento-card-label + h4 + p + features + stats + .bento-arrow

  **Part B -- Hero Orbs (index.html):**
  3. Add hero orbs as first child inside header.hero:
     div.hero-orbs containing div.hero-orb.hero-orb-1, div.hero-orb-2, div.hero-orb-3

  **Part C -- CSS (style.css) -- add as clearly-commented sections:**

  4. /* === HERO GRADIENT ORBS === */
     - .hero { position: relative; overflow: hidden; }
     - .hero-grid, .hero .container { position: relative; z-index: 2; }
     - .hero-orbs: position absolute, inset 0, pointer-events none, z-index 0
     - .hero-orb: position absolute, border-radius 50%, filter blur(80px), will-change transform
     - .hero-orb-1: 500x500px, radial-gradient rgba(197,237,54,0.15), top -100px right -100px, orbFloat1 18s ease-in-out infinite
     - .hero-orb-2: 350x350px, two-stop radial-gradient rgba(197,237,54,0.08)->rgba(180,220,40,0.05), bottom -50px left 5%, orbFloat2 22s reverse
     - .hero-orb-3: 250x250px, radial-gradient rgba(197,237,54,0.06), top 30% left 40%, orbFloat3 15s
     - @keyframes orbFloat1: 0%/100% translate(0) scale(1), 33% translate(-40px,30px) scale(1.08), 66% translate(20px,-20px) scale(0.95)
     - @keyframes orbFloat2: 0%/100% translate(0) scale(1), 50% translate(30px,-40px) scale(1.1)
     - @keyframes orbFloat3: 0%/100% translate(0), 50% translate(-20px,25px)
     - @media (prefers-reduced-motion: reduce) { .hero-orb { animation: none; } }

  5. /* === BENTO GRID === */
     - .bento-section: background #111, color white, padding 100px 0
     - .bento-section .section-label: color #c5ed36
     - .bento-section .section-title: color white
     - .bento-section .section-desc: color rgba(255,255,255,0.6)
     - .bento-grid: display grid, grid-template-columns repeat(3, 1fr), gap 20px, margin-top 48px
     - .bento-card--hero: grid-column 1/3, grid-row 1/3
     - .bento-card: background rgba(255,255,255,0.04), border 1px solid rgba(255,255,255,0.08), border-radius 20px, padding 32px, text-decoration none, color white, display flex, flex-direction column, transition all 0.3s ease, position relative, cursor pointer, opacity 0
     - .bento-card:hover: border-color rgba(197,237,54,0.5), transform scale(1.02), box-shadow 0 0 0 1px rgba(197,237,54,0.2) + 0 8px 32px rgba(197,237,54,0.12) + 0 20px 60px rgba(0,0,0,0.15)
     - .bento-card:focus-visible: outline 2px solid #c5ed36, outline-offset 2px
     - Inner styles: h3/h4 (white), p (rgba 0.6), .bento-feature (flex, gap 8px, 14px, rgba 0.7), .bento-feature i (#c5ed36 16px), .bento-stats (flex, gap 24px, margin-top auto, border-top), .bento-stat .stat-value (24px bold #c5ed36), .bento-stat .stat-label (12px uppercase rgba 0.5), .bento-arrow (absolute top-right, opacity 0, translateX -8px), .bento-card:hover .bento-arrow (opacity 1, translateX 0), .bento-card-icon (48px, bg rgba c5ed36 0.1, rounded 12px), .bento-card-label (12px uppercase, JetBrains Mono, #c5ed36)
     - Flow diagram: .bento-flow-diagram (flex, center, gap 12px, padding 20px, bg rgba 0.03, rounded 12px), .bento-flow-node (flex col, center), node-icon (40px, rounded 10px), LinkedIn bg rgba(10,102,194,0.15), AI bg rgba(197,237,54,0.15), .bento-flow-connector (24px, 2px, gradient), .bento-flow-connector::after (@keyframes flowPulse dot), .bento-flow-labels (flex gap 8px), label pills HOT/WARM/SKIP
     - Responsive: 1024px 2-col (hero full-width), 768px 1-col
     - @media (prefers-reduced-motion: reduce) { .bento-card { opacity: 1 !important; } .bento-flow-connector::after { animation: none; } }

  6. /* === CARD HOVER GLOW === */
     - .feature-card:hover: border-color rgba(197,237,54,0.5), box-shadow accent glow

  7. /* === CURSOR GLOW === */
     - .cursor-glow: position fixed, 400x400px, border-radius 50%, radial-gradient rgba(197,237,54,0.06), pointer-events none, z-index 1, will-change transform
     - @media (prefers-reduced-motion: reduce) { .cursor-glow { display: none; } }
  </action>
  <verify>
    <automated>cd "C:/Users/Dell 5520/Documents/AgenticWorkflows/Landing Page" &amp;&amp; grep -c "bento-grid" index.html &amp;&amp; grep -c "hero-orb" index.html &amp;&amp; grep -c "orbFloat" style.css &amp;&amp; grep -c "bento-section" style.css &amp;&amp; grep -c "cursor-glow" style.css</automated>
  </verify>
  <done>
  - Old solution-detail blocks replaced by bento grid in index.html
  - Hero orbs added inside header.hero
  - Full bento grid CSS: layout, hover glow, responsive, accessibility
  - Hero orb CSS with 3 keyframe animations
  - Card hover glow for feature cards
  - Cursor glow CSS
  - All prefers-reduced-motion guards
  </done>
</task>

<task type="auto">
  <name>Task 2: GSAP scroll reveals + cursor glow + bento stagger JS</name>
  <files>js/main.js</files>
  <action>
  Add three new functions to js/main.js and call them from the init flow:

  **1. initScrollReveal():**
  - Guard: check gsap/ScrollTrigger exist and prefers-reduced-motion not set
  - Animate below-fold section titles: use gsap.utils.toArray with selectors scoped to NOT hero (e.g., .bento-section .section-title, .features .section-title, .cta-section .section-title)
  - Each: gsap.fromTo(el, {y:30, opacity:0}, {y:0, opacity:1, duration:0.7, ease:power3.out, scrollTrigger:{trigger:el, start:top 85%, once:true}})
  - Also animate section-desc, section-label with slight delay offsets
  - CTA section children with stagger:0.1
  - logos-bar with y:20 fade

  **2. initBentoAnimations():**
  - Guard: gsap/ScrollTrigger + reduced-motion check
  - Hero card: gsap.fromTo .bento-card--hero, {y:60,opacity:0,scale:0.96} -> {y:0,opacity:1,scale:1,duration:0.8,ease:power3.out, scrollTrigger:{trigger:.bento-grid, start:top 80%, once:true}}
  - Smaller cards: gsap.fromTo .bento-card:not(.bento-card--hero), {y:50,opacity:0,scale:0.95} -> {y:0,opacity:1,scale:1,duration:0.6,stagger:0.12,ease:power3.out,delay:0.3, scrollTrigger same}

  **3. initCursorGlow():**
  - Guard: if ontouchstart in window return (desktop only)
  - Create div.cursor-glow, append to body
  - mousemove listener with requestAnimationFrame: set transform translate offset by -200px to center 400px element
  - Cursor glow (z-index:1) coexists with custom cursor ring (z-index:10000)

  **4. Call all three** in init flow after initFeaturesAnimations and initCustomCursor.

  **5. ScrollTrigger.refresh()** on window load if not present:
  window.addEventListener('load', () => { if (typeof ScrollTrigger !== 'undefined') ScrollTrigger.refresh(); });

  **6. Verify lucide.createIcons()** fires after DOM ready for bento card icons.
  </action>
  <verify>
    <automated>grep -c "initScrollReveal" "C:/Users/Dell 5520/Documents/AgenticWorkflows/Landing Page/js/main.js" &amp;&amp; grep -c "initBentoAnimations" "C:/Users/Dell 5520/Documents/AgenticWorkflows/Landing Page/js/main.js" &amp;&amp; grep -c "initCursorGlow" "C:/Users/Dell 5520/Documents/AgenticWorkflows/Landing Page/js/main.js"</automated>
  </verify>
  <done>
  - initScrollReveal() animates section titles/content on scroll (below-fold only)
  - initBentoAnimations() staggers bento cards (hero first, then smaller)
  - initCursorGlow() creates 400px radial gradient following mouse on desktop
  - ScrollTrigger.refresh() on window load
  - All functions called in init flow
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Bento grid replacing Como Funciona + full animation layer: hero orbs, GSAP scroll reveals, bento stagger, card hover glow, cursor glow.</what-built>
  <how-to-verify>
    1. Run npm start and open http://localhost:3000
    2. Hero: observe 2-3 gradient orbs slowly floating (watch 5+ seconds)
    3. Scroll slowly: sections fade-in/slide-up as they enter viewport
    4. Bento grid: 5 cards on dark bg, hero card 2x2, 4 smaller 1x1, stagger animation
    5. Hover bento card: green border glow, scale, arrow appears
    6. Hover feature card: same green glow
    7. Click bento card: navigates to soluciones.html section
    8. Move mouse: subtle green radial gradient follows cursor (desktop)
    9. Hero card flow diagram: LinkedIn -> IA -> HOT/WARM/SKIP with animated dots
    10. Mobile (375px): bento stacks vertically, no cursor glow, no overflow
  </how-to-verify>
  <resume-signal>Type approved or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. Hero orbs visible and animating
2. GSAP scroll reveals for all sections
3. Bento grid: 5 cards, dark bg, hero dominant, links work
4. Card hover glow on bento + feature cards
5. Cursor glow on desktop only
6. Responsive: 1024px 2-col, 768px 1-col
7. prefers-reduced-motion: all disabled, content visible
</verification>

<success_criteria>
- ANIM-01: Hero gradient orbs animate (3 orbs, CSS keyframes)
- ANIM-02: GSAP scroll reveals for all sections on index.html
- ANIM-03: Card hover glow on feature + bento cards (#c5ed36)
- ANIM-04: Cursor glow follows mouse on desktop only
- Bento grid replaces old Como Funciona (locked decision from CONTEXT.md)
</success_criteria>

<output>
After completion, create .planning/phases/02-landing-improvements/02-02-SUMMARY.md
</output>