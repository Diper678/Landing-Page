---
phase: 02-landing-improvements
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - index.html
  - style.css
  - js/main.js
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - ROI-01
  - ROI-02

must_haves:
  truths:
    - "User sees a 4-column comparison table (Sisteco vs DIY vs SDR vs No hacer nada) on index.html"
    - "Table uses green checkmarks and red X icons for visual scanning"
    - "Sisteco column is visually highlighted as recommended"
    - "ROI calculator accepts vendedores and ticket promedio inputs and shows estimated ROI"
    - "CLP values display with Chilean locale formatting (dot thousands)"
    - "Table and calculator are responsive on mobile"
  artifacts:
    - path: "index.html"
      provides: "Comparison table section and ROI calculator section HTML"
      contains: "comparison-table"
    - path: "style.css"
      provides: "Comparison table and ROI calculator CSS"
      contains: ".comparison"
    - path: "js/main.js"
      provides: "ROI calculator JS logic with CLP formatting"
      contains: "calcROI"
  key_links:
    - from: "js/main.js"
      to: "index.html #roi-vendedores, #roi-ticket"
      via: "DOM input event listeners"
      pattern: "roi-vendedores"
    - from: "index.html"
      to: "style.css .comparison-col.featured"
      via: "CSS class for highlighted Sisteco column"
      pattern: "comparison-col.*featured"
---

<objective>
Add a 4-column commercial comparison table and an interactive ROI calculator to index.html. The comparison table contrasts Sisteco vs DIY Stack vs Contratar SDR vs No hacer nada. The ROI calculator lets users input their team size and average ticket to see estimated returns.

Purpose: Strong commercial argument (HumbleOps-style comparison) + interactive engagement tool that demonstrates Sisteco's value in the user's own numbers.

Output: Two new sections in index.html (comparison + calculator) with supporting CSS and JS.
</objective>

<execution_context>
@C:/Users/Dell 5520/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dell 5520/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-landing-improvements/2-CONTEXT.md
@.planning/phases/02-landing-improvements/02-RESEARCH.md
@.planning/phases/02-landing-improvements/02-02-SUMMARY.md

@index.html (insert after bento grid section, before features section)
@style.css
@js/main.js

<interfaces>
<!-- ROI data from FINANCIAL_STRATEGY.md: -->
<!-- DIY stack cost: ~$1,253/month -->
<!-- SDR cost: CLP 1.2M/month -->
<!-- Sisteco base plan: $472/month (changed from $397 to $472 in como-funciona.html) -->
<!-- Conversion improvement: 5-7x -->
<!-- ROI stat: 391% (Forrester) -->
<!-- CLP/USD approximate rate: 900 -->

<!-- Comparison table row data from 02-RESEARCH.md Pattern 6 -->
<!-- Lucide icons for table: check, x, minus, zap -->

<!-- IMPORTANT: Plan 02 modified index.html (bento grid replaced Como Funciona), -->
<!-- style.css (bento + orbs + glow CSS), and js/main.js (GSAP + cursor glow). -->
<!-- Insert comparison table AFTER the bento-section, before features section. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build comparison table section</name>
  <files>index.html, style.css</files>
  <action>
1. **Add comparison table HTML to index.html** after the bento grid section (after `</section>` closing the como-funciona/bento section) and before the features section. Use a dark background section for visual contrast:

```html
<section class="comparison-section" id="comparativa">
  <div class="container">
    <span class="section-label">Por que Sisteco</span>
    <h2 class="section-title center">Elige tu camino</h2>
    <p class="section-desc">Compara las opciones reales para automatizar tu prospección B2B</p>

    <div class="comparison-table-wrapper">
      <div class="comparison-table">
        <!-- Table built as CSS grid, not HTML table, for responsive control -->
        <!-- Header row -->
        <!-- Data rows with 5 columns: label + 4 options -->
        <!-- Sisteco column gets .featured class -->
      </div>
    </div>
  </div>
</section>
```

2. **Table structure** -- use a CSS grid-based table (not HTML `<table>`) for better responsive behavior. Each row is a `.comparison-row` with 5 cells:
   - Column 1: `.comparison-label` (row label)
   - Column 2: DIY Stack
   - Column 3: Contratar SDR
   - Column 4: No hacer nada
   - Column 5: Sisteco (`.featured`)

3. **Header row** with column titles. Sisteco column header has a `.featured-badge` span saying "Recomendado".

4. **Data rows** (from 02-RESEARCH.md):
   | Row | DIY Stack | Contratar SDR | No hacer nada | Sisteco |
   |-----|-----------|---------------|---------------|---------|
   | Costo mensual | ~$1,253 USD | CLP 1.2M | $0 | Desde $472 USD |
   | Setup time | 2-4 semanas | 1-2 meses | -- | < 24 horas |
   | Time to first value | 2-4 semanas | 1-2 meses | Nunca | Hoy |
   | Canales activos | 1-2 | 1 | 0 | 5 |
   | Horario | 8h/dia | 8h/dia | -- | 24/7 |
   | Leads/semana | ~50 | ~30 | 0 | 200+ |
   | Tasa respuesta | 2-5% | 3-7% | -- | 15-25% |
   | Cash at risk | $3-8K setup | $14K/yr min | Oportunidad perdida | $0 setup |
   | ROI esperado | Incierto | Incierto | Negativo | 391% (Forrester) |

   Use Lucide icons: `check` (green) for positive Sisteco values, `x` (red) for negative competitor values, `minus` for neutral/missing.

5. **CSS for comparison table** (`/* === COMPARISON TABLE === */`):
   - `.comparison-section`: background #111, color white, padding 100px 0
   - `.comparison-section .section-label`: color #c5ed36
   - `.comparison-section .section-title, .section-desc`: color white / rgba
   - `.comparison-table-wrapper`: overflow-x auto (for mobile horizontal scroll)
   - `.comparison-table`: display grid, grid-template-columns 180px repeat(4, 1fr), gap 0, min-width 700px
   - `.comparison-row`: display contents (allows grid children to participate in parent grid)
   - `.comparison-cell`: padding 16px 20px, border-bottom 1px solid rgba(255,255,255,0.06), font-size 14px, display flex, align-items center
   - `.comparison-cell.label`: color rgba(255,255,255,0.7), font-weight 500, font-size 13px
   - `.comparison-cell.featured`: background rgba(197,237,54,0.04)
   - `.comparison-header .featured`: background rgba(197,237,54,0.08), border-radius 12px 12px 0 0, position relative
   - `.featured-badge`: position absolute, top -12px, left 50%, transform translateX(-50%), background #c5ed36, color #111, font-size 11px, font-weight 700, padding 2px 12px, border-radius 20px, white-space nowrap
   - Icon colors: `.icon-positive` color #c5ed36, `.icon-negative` color #ef4444, `.icon-neutral` color rgba(255,255,255,0.3)

6. **Responsive:** At 768px, the table stays grid-based but the wrapper allows horizontal scroll. Add a subtle scroll indicator or shadow on the right edge.
  </action>
  <verify>
    <automated>cd "C:/Users/Dell 5520/Documents/AgenticWorkflows/Landing Page" &amp;&amp; grep -c "comparison-table" index.html &amp;&amp; grep -c "comparison-section" style.css</automated>
  </verify>
  <done>
    - 4-column comparison table renders on index.html with dark background
    - Sisteco column highlighted with featured class and "Recomendado" badge
    - 9 data rows with correct values from FINANCIAL_STRATEGY.md
    - Green/red icons for visual scanning
    - Table scrolls horizontally on mobile
  </done>
</task>

<task type="auto">
  <name>Task 2: Build interactive ROI calculator</name>
  <files>index.html, style.css, js/main.js</files>
  <action>
1. **Add ROI calculator HTML** after the comparison table section in index.html:

```html
<section class="roi-section" id="calculadora-roi">
  <div class="container">
    <span class="section-label">Calculadora ROI</span>
    <h2 class="section-title center">Calcula tu retorno con Sisteco</h2>
    <p class="section-desc">Ingresa los datos de tu equipo y descubre cuanto puedes ganar</p>

    <div class="roi-calculator">
      <div class="roi-inputs">
        <div class="roi-input-group">
          <label for="roi-vendedores">Numero de vendedores</label>
          <input type="number" id="roi-vendedores" value="2" min="1" max="50" />
        </div>
        <div class="roi-input-group">
          <label for="roi-ticket">Ticket promedio (CLP)</label>
          <input type="number" id="roi-ticket" value="1500000" min="100000" step="100000" />
        </div>
      </div>

      <div class="roi-outputs">
        <div class="roi-output-card">
          <span class="roi-output-value" id="roi-leads">400</span>
          <span class="roi-output-label">Leads adicionales/mes</span>
        </div>
        <div class="roi-output-card">
          <span class="roi-output-value" id="roi-conversiones">12</span>
          <span class="roi-output-label">Conversiones estimadas</span>
        </div>
        <div class="roi-output-card">
          <span class="roi-output-value" id="roi-ingreso">$0</span>
          <span class="roi-output-label">Ingreso adicional estimado</span>
        </div>
        <div class="roi-output-card roi-output-card--highlight">
          <span class="roi-output-value" id="roi-porcentaje">0%</span>
          <span class="roi-output-label">ROI estimado</span>
        </div>
      </div>

      <p class="roi-disclaimer">Estimacion referencial basada en datos de mercado. Resultados reales pueden variar.</p>
    </div>
  </div>
</section>
```

2. **ROI Calculator JS** -- add to js/main.js:

```javascript
function initROICalculator() {
  const vendedoresInput = document.getElementById('roi-vendedores');
  const ticketInput = document.getElementById('roi-ticket');
  if (!vendedoresInput || !ticketInput) return;

  function formatCLP(amount) {
    return amount.toLocaleString('es-CL', { style: 'currency', currency: 'CLP', maximumFractionDigits: 0 });
  }

  function calcROI() {
    const vendedores = parseInt(vendedoresInput.value) || 2;
    const ticket = parseInt(ticketInput.value) || 1500000;
    const planCostCLP = 472 * 900; // USD to CLP approximate

    const leadsAdicionales = vendedores * 200;
    const conversionesAdicionales = Math.round(leadsAdicionales * 0.03); // conservative 3%
    const ingresoAdicional = conversionesAdicionales * ticket;
    const roiPorcentaje = Math.round(((ingresoAdicional - planCostCLP) / planCostCLP) * 100);

    document.getElementById('roi-leads').textContent = leadsAdicionales.toLocaleString('es-CL');
    document.getElementById('roi-conversiones').textContent = conversionesAdicionales;
    document.getElementById('roi-ingreso').textContent = formatCLP(ingresoAdicional);
    document.getElementById('roi-porcentaje').textContent = Math.max(0, roiPorcentaje) + '%';
  }

  vendedoresInput.addEventListener('input', calcROI);
  ticketInput.addEventListener('input', calcROI);
  calcROI(); // Initial calculation
}
```

Call `initROICalculator()` in DOMContentLoaded handler.

3. **ROI Calculator CSS** (`/* === ROI CALCULATOR === */`):
   - `.roi-section`: background #F8F7F5 (light, contrasts with dark comparison above), padding 100px 0
   - `.roi-calculator`: max-width 800px, margin 48px auto 0
   - `.roi-inputs`: display grid, grid-template-columns repeat(2, 1fr), gap 24px, margin-bottom 40px
   - `.roi-input-group label`: display block, font-size 14px, font-weight 500, color #666, margin-bottom 8px
   - `.roi-input-group input`: width 100%, padding 14px 18px, border 1px solid #e0e0e0, border-radius 12px, font-size 18px, font-family inherit, transition border-color 0.2s
   - `.roi-input-group input:focus`: border-color #c5ed36, outline none, box-shadow 0 0 0 3px rgba(197,237,54,0.15)
   - `.roi-outputs`: display grid, grid-template-columns repeat(4, 1fr), gap 16px
   - `.roi-output-card`: background white, border-radius 16px, padding 24px, text-align center, border 1px solid #e8e8e8, box-shadow 0 2px 8px rgba(0,0,0,0.04)
   - `.roi-output-card--highlight`: background #111, color white, border-color #111
   - `.roi-output-card--highlight .roi-output-value`: color #c5ed36
   - `.roi-output-value`: font-size 32px, font-weight 700, color #111, display block, margin-bottom 4px
   - `.roi-output-label`: font-size 13px, color #666
   - `.roi-disclaimer`: font-size 12px, color #999, text-align center, margin-top 24px, font-style italic

4. **Responsive:**
   - `@media (max-width: 768px)`: roi-inputs single column, roi-outputs 2x2 grid, roi-output-value font-size 24px

5. **Accessibility:** Labels are associated via `for` attribute, inputs have `min`/`max`/`step`, focus states have visible outlines.
  </action>
  <verify>
    <automated>cd "C:/Users/Dell 5520/Documents/AgenticWorkflows/Landing Page" &amp;&amp; grep -c "roi-calculator\|calcROI\|initROICalculator" js/main.js &amp;&amp; grep -c "roi-section" index.html</automated>
  </verify>
  <done>
    - ROI calculator renders with 2 inputs (vendedores, ticket) and 4 output cards
    - Changing inputs immediately recalculates outputs
    - CLP values display with Chilean locale (dot thousands separator)
    - ROI percentage card highlighted in dark with green accent
    - Calculator responsive on mobile (2x2 grid for outputs, single column inputs)
    - Disclaimer text visible below calculator
    - Comparison table + calculator sections both present on index.html
  </done>
</task>

</tasks>

<verification>
1. `npm start` -- scroll to comparison table on index.html
2. Verify 4 columns with correct data, Sisteco highlighted
3. Scroll to ROI calculator -- change vendedores to 5, verify outputs update
4. Change ticket to 2000000 -- verify CLP formatting correct
5. Mobile view (375px) -- table scrolls horizontally, calculator stacks
</verification>

<success_criteria>
- COMP-01: 4-column comparison table renders correctly with all 9 data rows
- COMP-02: Table is responsive (horizontal scroll on mobile)
- ROI-01: Calculator shows output when inputs change (immediate update)
- ROI-02: CLP values format correctly with es-CL locale
</success_criteria>

<output>
After completion, create `.planning/phases/02-landing-improvements/02-03-SUMMARY.md`
</output>
